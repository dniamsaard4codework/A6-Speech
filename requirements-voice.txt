# Exercise 4 (voice cloning) extras — installed ON TOP of the main .venv (single env).
# OpenVoice V2 + MeloTTS. We keep OpenVoice's real `se_extractor.get_se` (silero-VAD path),
# so we install its front-end: faster-whisper + whisper-timestamped + PyAV, plus a
# wheel-shipped ffmpeg via imageio-ffmpeg (no system ffmpeg / no compiling). whisper-timestamped
# tries to pull numpy>=2; we re-pin numpy==1.26.4 afterwards (see scripts/setup.ps1) because
# wav2vec2 (Ex3) + MeloTTS need numpy<2. gradio / tensorboard are still dropped (unused).

# --- OpenVoice se_extractor.get_se front-end (Ex 4b) ---
faster-whisper==1.2.1
whisper-timestamped==1.15.9
av==17.1.0
imageio-ffmpeg==0.6.0

# --- OpenVoice V2 converter runtime ---
wavmark==0.0.3
pydub==0.25.1
eng_to_ipa==0.0.2
inflect==7.0.0
unidecode==1.3.7
pypinyin==0.50.0
cn2an==0.5.22
jieba==0.42.1
langid==1.1.6

# --- MeloTTS base-speaker frontend ---
cached_path
txtsplit
num2words==0.5.12
unidic_lite==1.0.8
mecab-python3
pykakasi==2.2.1
fugashi==1.3.0
g2p_en==2.1.0
anyascii==0.3.2
jamo==0.4.1
gruut[es,fr]==2.2.3
g2pkk>=0.1.1
loguru==0.7.2

# OpenVoice + MeloTTS themselves are installed as editable, --no-deps, from third_party/:
#   uv pip install --python .venv/Scripts/python.exe --no-deps -e third_party/OpenVoice -e third_party/MeloTTS
