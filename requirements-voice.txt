# Exercise 4 (voice cloning) extras — installed ON TOP of the main .venv (single env).
# OpenVoice V2 + MeloTTS. We deliberately DROP faster-whisper / whisper-timestamped / PyAV /
# gradio / tensorboard: the heavy se_extractor VAD path is replaced by a lightweight
# chunk-and-average tone-color extractor (a6_speech.voice_clone.extract_se), so none of the
# OpenVoice runtime code that we actually call needs them.

# --- OpenVoice V2 converter runtime ---
wavmark==0.0.3
pydub==0.25.1
eng_to_ipa==0.0.2
inflect==7.0.0
unidecode==1.3.7
pypinyin==0.50.0
cn2an==0.5.22
jieba==0.42.1
langid==1.1.6

# --- MeloTTS base-speaker frontend ---
cached_path
txtsplit
num2words==0.5.12
unidic_lite==1.0.8
mecab-python3
pykakasi==2.2.1
fugashi==1.3.0
g2p_en==2.1.0
anyascii==0.3.2
jamo==0.4.1
gruut[es,fr]==2.2.3
g2pkk>=0.1.1
loguru==0.7.2

# OpenVoice + MeloTTS themselves are installed as editable, --no-deps, from third_party/:
#   uv pip install --python .venv/Scripts/python.exe --no-deps -e third_party/OpenVoice -e third_party/MeloTTS
